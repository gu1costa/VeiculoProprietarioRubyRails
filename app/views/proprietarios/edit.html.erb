<% content_for :title, "Editar Proprietário - DETRAN" %>
<% content_for :header_title, "Editar Proprietário" %>

<% content_for :header_actions do %>
  <%= link_to "← Voltar para Lista", proprietarios_path, class: "back-btn" %>
<% end %>

<div class="page-center">
  <h2 class="page-heading">Atualizar Dados do Proprietário</h2>

  <!-- CARD: Dados do proprietário -->
  <div class="panel">
    <div class="panel-head">
      <h3>Informações do Proprietário</h3>
    </div>

    <div class="panel-body">
      <%= form_with model: @proprietario, local: true do |f| %>
        <% if @proprietario.errors.any? %>
          <div class="alert alert-error">
            <strong>Não foi possível salvar:</strong>
            <ul style="margin-left:18px;margin-top:8px;">
              <% @proprietario.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <label>CPF/CNPJ <span class="required">*</span></label>
          <%= f.text_field :cpf_cnpj, class: "form-control", maxlength: 14, required: true, inputmode: "numeric", readonly: true, style: "background-color: #f5f5f5; cursor: not-allowed;" %>
<!--          <small style="color: #6b7280; font-size: 12px; margin-top: 4px; display: block;">
            ⚠️ CPF/CNPJ não pode ser alterado para manter a integridade do sistema
          </small>-->
        </div>

        <div class="form-group">
          <label>Nome Completo <span class="required">*</span></label>
          <%= f.text_field :nome, class: "form-control", maxlength: 100, required: true %>
        </div>

        <div class="form-group">
          <label>Endereço Completo <span class="required">*</span></label>
          <%= f.text_field :endereco, class: "form-control", required: true %>
        </div>

        <div class="panel-actions">
          <%= f.submit "💾 Salvar Alterações", class: "btn" %>
          <%= link_to "Cancelar", proprietarios_path, class: "btn btn-secondary" %>
        </div>
      <% end %>
    </div>
  </div>

  <!-- CARD: Veículos -->
  <div class="panel">
    <div class="panel-head">
      <h3>Veículos Cadastrados</h3>
    </div>

    <div class="panel-body">
      <% if @proprietario.veiculos.any? %>
        <div class="vehicle-list">
          <% @proprietario.veiculos.order(:id).each do |v| %>
            <div class="vehicle-item">
              <div class="vehicle-left">
                <div class="vehicle-plate">
                  <span class="vehicle-icon">🚗</span>
                  <strong><%= v.placa %></strong>
                </div>
                <div class="vehicle-sub">RENAVAM: <%= v.renavam %></div>
              </div>

              <div class="vehicle-actions">
                <%= link_to "✏️ Editar",
                            edit_proprietario_veiculo_path(@proprietario, v),
                            class: "btn btn-sm btn-secondary" %>

                <%= button_to "🗑️ Remover",
                              proprietario_veiculo_path(@proprietario, v),
                              method: :delete,
                              form: { class: "inline-form" },
                              class: "btn btn-sm btn-danger",
                              data: { turbo_confirm: "Deseja realmente remover este veículo?" } %>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="empty-state">
          <strong>Nenhum veículo cadastrado para este proprietário.</strong>
        </div>
      <% end %>

      <% if @veiculo&.errors&.any? %>
        <div class="alert alert-error">
          <strong>Não foi possível salvar:</strong>
          <ul style="margin-left: 18px; margin-top: 8px;">
            <% @veiculo.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

    </div>
  </div>
</div>

<script>
    // Forçar maiúsculas e bloquear caracteres inválidos no nome
    document.getElementById('proprietario_nome')?.addEventListener('input', function(e) {
        // Remove números e símbolos, permite apenas letras e espaços
        e.target.value = e.target.value.replace(/[^a-zA-ZÀ-ÿ\s]/g, '').toUpperCase();
    });
    
    // Forçar maiúsculas e bloquear caracteres inválidos no endereço
    document.getElementById('proprietario_endereco')?.addEventListener('input', function(e) {
        // Permite letras, números, espaços e caracteres básicos de endereço
        e.target.value = e.target.value.replace(/[^a-zA-ZÀ-ÿ0-9\s,\-\.\º\/]/g, '').toUpperCase();
    });
</script>
