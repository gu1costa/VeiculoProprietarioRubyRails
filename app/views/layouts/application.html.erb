<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Sistema DETRAN" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
<div class="header">
  <div class="header-content">
    <div class="logo">
      <h1><%= content_for(:header_title) || "Sistema DETRAN" %></h1>
    </div>

    <% if content_for?(:header_actions) %>
      <div class="header-actions">
        <%= yield :header_actions %>
      </div>
    <% end %>
  </div>
</div>

<div class="main-content">
  <% if content_for?(:page_title) %>
    <h2 class="page-title"><%= yield :page_title %></h2>
  <% end %>

  <% if notice.present? %>
    <div class="alert alert-success"><%= notice %></div>
  <% end %>

  <% if alert.present? %>
    <div class="alert alert-error"><%= alert %></div>
  <% end %>

  <%= yield %>
</div>

<div class="footer">
  <p><%= content_for(:footer) || "Sistema DETRAN - Versão 1.0 | © 2026 Departamento Estadual de Trânsito" %></p>
</div>
</body>
</html>
