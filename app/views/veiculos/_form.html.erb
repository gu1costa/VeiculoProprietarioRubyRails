<%= form_with(model: [@proprietario, @veiculo]) do |form| %>
  <% if @veiculo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@veiculo.errors.count, "erro") %> impediram o veículo de ser salvo:</h2>

      <ul>
        <% @veiculo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :placa, style: "display: block" %>
    <%= form.text_field :placa %>
  </div>

  <div>
    <%= form.label :renavam, style: "display: block" %>
    <%= form.text_field :renavam %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
